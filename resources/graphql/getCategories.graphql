query GetCategories($tag: String) {
  pages(where: {tag: $tag}) {
    nodes {
      id
      databaseId
      title
      slug
      featuredImage {
        node {
          mediaDetails {
            sizes {
              name
              sourceUrl
            }
          }
          altText
        }
      }
    }
  }
}