query GetCategories($tag: String) {
  pages(where: {tag: $tag}) {
    nodes {
      id
      title
      featuredImage {
        node {
          mediaDetails {
            sizes {
              name
              sourceUrl
            }
          }
          altText
        }
      }
    }
  }
}